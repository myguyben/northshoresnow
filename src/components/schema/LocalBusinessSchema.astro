---
import { SITE, AREA_LINKS } from '../../lib/site'

const schema = {
  '@context': 'https://schema.org',
  '@type': 'LocalBusiness',
  '@id': `${SITE.url}/#business`,
  name: SITE.name,
  legalName: SITE.legalName,
  url: SITE.url,
  telephone: '+1-604-990-7072',
  email: SITE.email,
  image: `${SITE.url}${SITE.defaultOgImage}`,
  priceRange: '$$',
  // Ties the site to the Google Business Profile that carries the reviews and
  // map pin. No aggregateRating here on purpose: self-served review markup is
  // ineligible for rich results and risks a manual action.
  sameAs: [SITE.googleBusinessProfile],
  hasMap: SITE.googleBusinessProfile,
  address: {
    '@type': 'PostalAddress',
    streetAddress: SITE.address.street,
    addressLocality: SITE.address.city,
    addressRegion: SITE.address.region,
    postalCode: SITE.address.postalCode,
    addressCountry: SITE.address.country,
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: SITE.geo.lat,
    longitude: SITE.geo.lng,
  },
  areaServed: AREA_LINKS.map((area) => ({ '@type': 'Place', name: area.name })),
  openingHoursSpecification: {
    '@type': 'OpeningHoursSpecification',
    dayOfWeek: ['Monday', 'Tuesday', 'Wednesday', 'Thursday', 'Friday', 'Saturday', 'Sunday'],
    opens: '00:00',
    closes: '23:59',
  },
  description:
    'Snow removal, plowing, salting and de-icing for strata, property managers, businesses and homeowners across North Vancouver, West Vancouver, Downtown Vancouver, Burnaby and Richmond.',
}
---

<script type="application/ld+json" set:html={JSON.stringify(schema)} />
